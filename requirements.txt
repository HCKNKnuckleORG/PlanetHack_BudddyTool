# Core dependencies
# Requires Python >= 3.9

# GUI Framework
# tkinter is included with Python
Pillow>=10.0.0

# Network & Security Tools
requests>=2.31.0
urllib3>=2.0.0
aiohttp>=3.9.0
httpx>=0.25.0
scapy>=2.5.0
python-nmap>=0.7.1

# Web Security Testing
beautifulsoup4>=4.12.0
lxml>=4.9.0
selenium>=4.15.0
playwright>=1.40.0

# SQL Injection
# sqlmap is an external tool, install separately
# pip install sqlmap (if available) or use as CLI tool

# XSS Testing
# xsser is an external tool, not a Python package

# Directory Brute Forcing
# dirsearch and gobuster are external tools, not Python packages
# Install separately: pip install dirsearch (if available) or use as CLI tools

# Subdomain Enumeration
# sublist3r can be installed separately
dnspython>=2.4.0

# API Testing
httpx[http2]>=0.25.0
graphql-core>=3.2.0

# JWT Testing
pyjwt>=2.8.0
jwt>=1.3.1

# XML/XXE
defusedxml>=0.7.1
lxml>=4.9.0

# Deserialization
pyyaml>=6.0.1
# pickle5 is built into Python 3.8+, no separate install needed

# Cryptography
cryptography>=41.0.0
pycryptodome>=3.19.0

# Logging
loguru>=0.7.2
colorlog>=6.8.0

# Configuration
pyyaml>=6.0.1
python-dotenv>=1.0.0

# Database
sqlalchemy>=2.0.0

# Web UI (Flask)
flask>=3.0.0

# Utilities
tqdm>=4.66.0
rich>=13.7.0
click>=8.1.7

# Development
pytest>=7.4.0
pytest-cov>=4.1.0
black>=23.11.0
flake8>=6.1.0
mypy>=1.7.0

