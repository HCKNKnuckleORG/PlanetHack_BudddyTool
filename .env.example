# PlanetHack Environment Variables (optional)
# Copy to .env and customize. Never commit .env to version control.

# Logging
# LOG_LEVEL=INFO

# Environment (dev, build, prod)
# ENV=dev

# PostgreSQL (when using docker-compose --profile with-db)
# POSTGRES_PASSWORD=your-secure-password
